<table>
    <% var i; for(i = 0; i < Quizes.length; i++){ %>
    <tr>
        <td>
            <a href="quizes/<%= Quizes[i].id %>">
                <%= Quizes[i].pregunta %>
            </a>
        </td>
        <td>
            <a href="quizes/<%= Quizes[i].id %>/edit">
               <button>editar</button> 
            </a>
        </td>
        <td>
            <form method="post" action="quizes/<%Quizes[i].id%>?_method=delete"><button type="submit" onclick="return confirm('Borrar: <%Quiz[i].pregunta%>');">borrar</button></form>
        </td>
    </tr>
    <% } %>
</table>
<p><a href="/quizes/new"><button>Crear pregunta</button></a></p>